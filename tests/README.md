# Tests — Phase 4

Validation and testing scripts for AVD deployments.

Reserved for post-deployment validation (session host health, AVD agent status, connectivity checks).

## Workflow

```
config/  →  infrastructure/  →  deploy/  →  configure/  →  tests/ (you are here)
```
