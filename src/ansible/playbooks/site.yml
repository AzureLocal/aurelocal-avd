---
# site.yml – Master playbook for AVD on Azure Local
# Run with:  ansible-playbook site.yml -i inventory.example.yml

- name: Deploy AVD Control Plane
  hosts: localhost
  connection: local
  gather_facts: false
  tags:
    - control-plane
  roles:
    - avd-control-plane

- name: Configure AVD Networking
  hosts: localhost
  connection: local
  gather_facts: false
  tags:
    - networking
  roles:
    - avd-networking

- name: Deploy AVD Session Hosts on Azure Local
  hosts: localhost
  connection: local
  gather_facts: false
  tags:
    - session-hosts
  roles:
    - avd-session-hosts

- name: Configure Identity & RBAC
  hosts: localhost
  connection: local
  gather_facts: false
  tags:
    - identity
  roles:
    - avd-identity

- name: Configure FSLogix Profile Containers
  hosts: localhost
  connection: local
  gather_facts: false
  tags:
    - fslogix
  roles:
    - avd-fslogix

- name: Deploy AVD Scaling Plan
  hosts: localhost
  connection: local
  gather_facts: false
  tags:
    - scaling
  roles:
    - avd-scaling

- name: Configure AVD Monitoring & Diagnostics
  hosts: localhost
  connection: local
  gather_facts: false
  tags:
    - monitoring
  roles:
    - avd-monitoring
